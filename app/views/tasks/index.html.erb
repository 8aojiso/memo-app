<%= link_to "CREATE NEW TASK", "/tasks/new" , class: "btn btn-primary"%>
<h2> 【TASK LIST】 </h2>
<% @tasks.each do |task| %>
    <p> ・<%= task.title %><%= link_to "DELETE" , "/tasks/" + task.id.to_s, method: :delete %></p>
<% end %>